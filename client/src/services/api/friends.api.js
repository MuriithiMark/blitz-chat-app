import { SERVER_URL } from "../../utils/constants"

const sendFriendRequest = async () => {
    try {
        // TODO add code here
        const FRIENDS_REQUEST_URL = `${SERVER_URL}`
    } catch (error) {
        throw error
    }
}

export {
    sendFriendRequest
}